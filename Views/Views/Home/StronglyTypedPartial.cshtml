@model IEnumerable<string>

<div class="bg-info">
    This is the message from the strong partial view
    <ul>
        @foreach (var str in Model)
        {
            <li>@str</li>
        }
    </ul>
</div>